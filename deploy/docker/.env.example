# Copy to .env alongside docker-compose.yaml (optional overrides).

# Container image (override for local builds)
# ZIGBEELENS_IMAGE=ghcr.io/zigbeelens/zigbeelens:latest

# Logging: debug | info | warning | error
ZIGBEELENS_LOG_LEVEL=info

# Timezone for log timestamps and UI display
TZ=UTC
